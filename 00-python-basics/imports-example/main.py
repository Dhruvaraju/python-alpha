# To import a specific function from a file, use as below
from lib.module import add
# to import all functions from a file, use as below
import lib.arthematic_ops as ops
print(add(1, 2))
print(ops.multiply(1, 2))